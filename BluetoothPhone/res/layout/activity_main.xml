<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.lkfor.bluetoothphone.MainActivity$PlaceholderFragment" >

    <Button
        android:id="@+id/btOn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:onClick="on"
        android:text="On" />

    <ListView
        android:id="@+id/lstList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btOff"
        android:layout_centerHorizontal="true" >

    </ListView>

    <Button
        android:id="@+id/btOff"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/btOn"
        android:layout_alignBottom="@+id/btOn"
        android:layout_toRightOf="@+id/btOn"
        android:onClick="off"
        android:text="Off" />

    <Button
        android:id="@+id/btList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/lstList"
        android:layout_alignParentRight="true"
        android:onClick="list"
        android:text="List" />

    <Button
        android:id="@+id/btVisible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/lstList"
        android:layout_toLeftOf="@+id/btList"
        android:onClick="visible"
        android:text="Visible" />

</RelativeLayout>